# OpenAI plugin

This directory will package `nutty-capture` and `nutty-recall` after the MCP
tool contracts and remote endpoint are working. It does not contain a manifest
yet, so the repository does not advertise an unusable plugin.
