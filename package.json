{
  "name": "openclaw-nutty",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-platform, user-controlled memory for AI conversations",
  "type": "module",
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build && pnpm run build:plugin",
    "build:plugin": "node scripts/build-openai-plugin.mjs",
    "typecheck": "pnpm -r --if-present typecheck",
    "test:unit": "pnpm -r --if-present test",
    "test:plugin": "bash scripts/smoke-openai-plugin.sh",
    "test": "pnpm run build && pnpm run test:unit && pnpm run test:plugin",
    "check": "pnpm run build && pnpm run typecheck && pnpm run test:unit && pnpm run test:plugin"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
